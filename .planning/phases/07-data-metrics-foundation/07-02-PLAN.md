---
phase: 07-data-metrics-foundation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [src/predictive_poultry_systems/analytics/sinks.py, main.py, tests/test_data_metrics.py]
autonomous: true
requirements: [MET-INF-04, MET-01, MET-02, MET-03]

must_haves:
  truths:
    - "Simulation metrics (resources, kpis) are written to SQLite at regular simulation intervals."
    - "All simulation events are persistent in the SQLite database."
    - "Test suite validates data integrity after a simulation execution."
  artifacts:
    - path: "src/predictive_poultry_systems/analytics/sinks.py"
      provides: "MetricSink salabim component for DB persistence"
    - path: "tests/test_data_metrics.py"
      provides: "Automated verification of the metrics foundation"
  key_links:
    - from: "src/predictive_poultry_systems/analytics/sinks.py"
      to: "src/predictive_poultry_systems/analytics/database.py"
      via: "DatabaseManager.log_metrics()"
    - from: "main.py"
      to: "src/predictive_poultry_systems/analytics/logging.py"
      via: "logger configuration at env creation"
---

<objective>
Integrate the logging and database foundation into the simulation loop, enabling real-time metrics persistence and structured event tracking.

Purpose: Provide a reliable data foundation for performance analysis and fulfillment optimization.
Output: MetricSink component, integrated main.py, and verification tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-metrics-foundation/07-01-SUMMARY.md
@src/predictive_poultry_systems/analytics/database.py
@src/predictive_poultry_systems/analytics/logging.py
@src/predictive_poultry_systems/agents/simulation.py
@main.py

<interfaces>
From src/predictive_poultry_systems/analytics/database.py (created in Plan 01):
```python
class DatabaseManager:
    def create_run(self, seed: int, config: str = "") -> int: ...
    def log_event(self, run_id: int, sim_time: float, level: str, message: str): ...
    def log_resource(self, run_id: int, sim_time: float, name: str, avail: int, claimed: int, queue: int): ...
    def log_kpi(self, run_id: int, sim_time: float, name: str, value: float): ...
    def close(self): ...
```

From salabim (for MetricSink):
```python
class Component(sim.Component):
    def process(self):
        while True:
            # perform action
            yield self.hold(duration)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Metric Sinking Component Implementation</name>
  <files>src/predictive_poultry_systems/analytics/sinks.py</files>
  <action>
    1. Create `MetricSink(sim.Component)` which takes `db: DatabaseManager` and `run_id: int`.
    2. In its `process()` loop, yield a `hold(5.0)` (simulated minutes).
    3. Every interval, iterate over `env.kiosks`, `env.fryers`, and `env.holding_cabinet` to log their states.
    4. Access `env.fulfillment_manager` to log KPI metrics (CSI, SMI, Revenue, SoS).
    5. Handle potential `AttributeError` if some monitors are not initialized yet.
  </action>
  <verify>
    <automated>uv run ruff check src/predictive_poultry_systems/analytics/sinks.py</automated>
  </verify>
  <done>MetricSink component is implemented and correctly samples simulation objects.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-End Main Integration</name>
  <files>main.py</files>
  <action>
    1. Update `run_simulation` to:
       - Initialize `DatabaseManager` using `DEFAULT_DB_PATH`.
       - Create a new `run_id` for the current simulation.
       - Initialize `setup_simulation_logger(env)`.
       - Add a `loguru` sink to redirect all logs to the SQLite `simulation_logs` table (using `db.log_event`).
       - Instantiate the `MetricSink`.
       - Ensure `db.close()` is called in a `finally` block or after `env.run()`.
    2. Replace direct `print` statements with `logger.info`, `logger.debug`, etc.
  </action>
  <verify>
    <automated>uv run main.py --till 50</automated>
  </verify>
  <done>Simulation runs with structured logging and persistent metrics to SQLite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persistence Validation Suite</name>
  <files>tests/test_data_metrics.py</files>
  <behavior>
    - Run simulation: Execute a short sim run.
    - Database check: Query the SQLite file and verify row counts > 0 for `simulation_logs` and `resource_metrics`.
    - Run metadata: Verify `runs` table has correct seed and start time.
    - Consistency: Verify `sim_time` in logs increases or stays same.
  </behavior>
  <action>
    Create a new test file `tests/test_data_metrics.py` to verify the entire persistence chain.
    Mocking the simulation or running a minimal one is acceptable.
  </action>
  <verify>
    <automated>uv run pytest tests/test_data_metrics.py</automated>
  </verify>
  <done>Test suite confirms that data foundation correctly captures and persists simulation reality.</done>
</task>

</tasks>

<verification>
Run `uv run main.py` followed by checking the SQLite file with `sqlite3 simulation_metrics.sqlite "SELECT count(*) FROM simulation_logs"`.
</verification>

<success_criteria>
- SQLite database is populated with multiple entries across all tables after a run.
- Console output shows formatted loguru logs with simulation time.
- All tests in `tests/test_data_metrics.py` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-metrics-foundation/07-02-SUMMARY.md`
</output>
