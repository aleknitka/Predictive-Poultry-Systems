---
phase: 07-data-metrics-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/predictive_poultry_systems/config.py, src/predictive_poultry_systems/analytics/logging.py, src/predictive_poultry_systems/analytics/database.py]
autonomous: true
requirements: [MET-INF-01, MET-INF-02, MET-INF-03]

must_haves:
  truths:
    - "loguru is successfully configured with a custom simulation time patch."
    - "SQLite database manager initializes with WAL mode and the specified relational schema."
    - "Simulation configuration includes a default database path."
  artifacts:
    - path: "src/predictive_poultry_systems/analytics/logging.py"
      provides: "Structured logging with env.now() integration"
    - path: "src/predictive_poultry_systems/analytics/database.py"
      provides: "Relational metrics storage using sqlite3"
  key_links:
    - from: "src/predictive_poultry_systems/analytics/logging.py"
      to: "salabim.Environment"
      via: "patching logger with env.now()"
---

<objective>
Establish the foundational infrastructure for structured logging and persistent data storage using loguru and SQLite.

Purpose: Enable simulation-aware event tracking and relational storage of metrics for downstream analysis.
Output: Logging configuration and DatabaseManager.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-metrics-foundation/07-CONTEXT.md
@.planning/phases/07-data-metrics-foundation/07-RESEARCH.md
@src/predictive_poultry_systems/config.py

<interfaces>
From salabim (internal):
- Environment.now() -> float: Returns current simulation time.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logging Infrastructure Setup</name>
  <files>pyproject.toml, src/predictive_poultry_systems/config.py, src/predictive_poultry_systems/analytics/logging.py</files>
  <action>
    1. Add `loguru` to `pyproject.toml` dependencies.
    2. Add `DEFAULT_DB_PATH = "simulation_metrics.sqlite"` to `src/predictive_poultry_systems/config.py`.
    3. Create `src/predictive_poultry_systems/analytics/logging.py` implementing `setup_simulation_logger(env)`.
    4. Per D-01, use `logger.patch()` to inject `env.now()` into log records.
    5. Configure a format that highlights simulation time: `[<sim_time>] <level> <message>`.
  </action>
  <verify>
    <automated>uv run python -c "from predictive_poultry_systems.analytics.logging import setup_simulation_logger; import salabim as sim; env = sim.Environment(); setup_simulation_logger(env); from loguru import logger; logger.info('Test log')"</automated>
  </verify>
  <done>Loguru is integrated and correctly displays simulation time (0.00) in output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Database Manager & Schema Implementation</name>
  <files>src/predictive_poultry_systems/analytics/database.py</files>
  <behavior>
    - Initialization: Creates the SQLite file if missing.
    - Performance: Enables WAL mode and synchronous=NORMAL per RESEARCH.md.
    - Schema: Creates tables `runs`, `simulation_logs`, `resource_metrics`, and `kpi_metrics` (for CSI/SMI).
    - Session: `create_run()` inserts metadata and returns a `run_id`.
  </behavior>
  <action>
    Implement `DatabaseManager` in `src/predictive_poultry_systems/analytics/database.py` using standard `sqlite3` (D-03).
    Follow the relational schema defined in RESEARCH.md.
    Ensure `runs` table tracks start time, seed, and optional config.
  </action>
  <verify>
    <automated>uv run python -c "from predictive_poultry_systems.analytics.database import DatabaseManager; db = DatabaseManager(':memory:'); print('Schema OK') if db.conn.execute(\"SELECT name FROM sqlite_master WHERE type='table' AND name='runs'\").fetchone() else exit(1)"</automated>
  </verify>
  <done>DatabaseManager successfully initializes schema and handles run creation.</done>
</task>

</tasks>

<verification>
Check that `loguru` is added to pyproject.toml and the `analytics` module contains the new foundational files.
</verification>

<success_criteria>
- loguru patched with env.now() produces formatted console output.
- DatabaseManager creates a valid SQLite database with the required normalized tables.
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-metrics-foundation/07-01-SUMMARY.md`
</output>
