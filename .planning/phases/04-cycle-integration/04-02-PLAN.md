---
phase: 04-cycle-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [main.py, tests/test_main.py]
autonomous: true
requirements: [SIM-LOOP-01, SIM-LOOP-02]
must_haves:
  truths:
    - "main.py initializes a salabim.Environment and all necessary Resources (kiosks, machines) and Stores (cabinets)."
    - "Simulation trace shows a complete customer lifecycle (arrival -> finish)."
    - "Staff components react to customer demand by executing production processes."
  artifacts:
    - path: "main.py"
      provides: "Integrated simulation loop"
      contains: ["sim.Environment", "sim.Resource", "sim.Store", "Customer", "Staff"]
    - path: "tests/test_main.py"
      provides: "Full cycle smoke test"
      contains: "test_full_simulation_cycle"
  key_links:
    - from: "main.py"
      to: "src/predictive_poultry_systems/agents/customers/behavior.py"
      via: "instantiation"
    - from: "main.py"
      to: "src/predictive_poultry_systems/agents/staff/behavior.py"
      via: "instantiation"
---

<objective>
Execute the full simulation cycle end-to-end by integrating the behavioral agents, machine resources, and operational stores in main.py. This achieves the core goal of Phase 4: a working fulfillment node digital twin.

Purpose: To demonstrate the end-to-end flow of the simulation and verify that all components work together.
Output: A functional main.py with integrated processes and a comprehensive test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-cycle-integration/04-RESEARCH.md
@.planning/phases/04-cycle-integration/04-01-PLAN.md
@main.py
@src/predictive_poultry_systems/agents/customers/behavior.py
@src/predictive_poultry_systems/agents/staff/behavior.py

<interfaces>
From src/predictive_poultry_systems/agents/customers/behavior.py:
```python
class Customer(sim.Component):
    def process(self): ...
```

From src/predictive_poultry_systems/agents/staff/behavior.py:
```python
class Staff(sim.Component):
    def process(self): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup resources and stores in main.py</name>
  <files>main.py</files>
  <action>
    Update `run_simulation` in `main.py` to:
    1. Initialize `sim.Resource` for ordering kiosks (using `BaseMachine` parameters from Phase 2).
    2. Initialize `sim.Resource` for cooking equipment (fryers/grills).
    3. Initialize `sim.Store` for the holding cabinet (Pattern 2 from research).
    4. Set up an arrival generator (a simple `sim.Component` that creates `Customer` components at stochastic intervals).
  </action>
  <verify>
    <automated>uv run main.py --help || exit 0</automated>
  </verify>
  <done>Simulation environment is populated with the necessary physical and operational infrastructure.</done>
</task>

<task type="auto">
  <name>Task 2: Connect the full fulfillment loop</name>
  <files>main.py</files>
  <action>
    Update the `main.py` entry point to instantiate a set of `Staff` components and start the customer arrival process.
    Ensure that `Staff` and `Customer` instances share the same `sim.Store` (cabinet) and `sim.Resource` (kiosks/machines) objects.
    Enable `trace=True` in `sim.Environment` to allow for event tracking.
  </action>
  <verify>
    <automated>uv run main.py | grep -E "Customer|Staff|Cabinet"</automated>
  </verify>
  <done>Full fulfillment loop is integrated and producing event traces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement automated trace verification</name>
  <files>tests/test_main.py</files>
  <behavior>
    - The test should run a short simulation (e.g., 500 timesteps).
    - It should verify that at least one customer completes their cycle.
    - It should verify that the `holding_cabinet` was used (item entered and left).
    - It should verify that `Staff` performed a production cycle if needed.
  </behavior>
  <action>
    Update `tests/test_main.py` with a new test case `test_full_simulation_cycle`.
    Use a mock or a small set of predefined parameters for machines and agents to ensure deterministic behavior where possible.
  </action>
  <verify>
    <automated>uv run pytest tests/test_main.py</automated>
  </verify>
  <done>Simulation loop is verified through automated smoke tests.</done>
</task>

</tasks>

<verification>
### Integrated Simulation Verification
- [ ] `uv run main.py` executes without errors and shows a full trace.
- [ ] Trace includes customer arrival, ordering, cooking (if store empty), and delivery.
- [ ] `uv run pytest tests/test_main.py` passes all tests.
</verification>

<success_criteria>
- The simulation accurately models a full end-to-end fulfillment cycle.
- Behavioral agents correctly interact with operational assets and processes.
- The system produces a verifiable timeline of events.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cycle-integration/04-02-SUMMARY.md`
</output>
