---
phase: 04-cycle-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/predictive_poultry_systems/agents/customers/behavior.py, src/predictive_poultry_systems/agents/staff/behavior.py]
autonomous: true
requirements: [AGT-CUST-01, AGT-CUST-02, AGT-STAF-01]
must_haves:
  truths:
    - "Customer component implements salabim.Component and uses stochastic arrival patterns."
    - "Staff component implements salabim.Component and uses machine/process models for task execution."
    - "Agents correctly reference and utilize Pydantic models from Phase 2 (BaseCustomer, BaseStaff)."
  artifacts:
    - path: "src/predictive_poultry_systems/agents/customers/behavior.py"
      provides: "Stochastic customer behavior in the simulation"
      contains: "class Customer(sim.Component)"
    - path: "src/predictive_poultry_systems/agents/staff/behavior.py"
      provides: "Labor-driven fulfillment logic"
      contains: "class Staff(sim.Component)"
  key_links:
    - from: "src/predictive_poultry_systems/agents/customers/behavior.py"
      to: "salabim.Component"
      via: "inheritance"
    - from: "src/predictive_poultry_systems/agents/staff/behavior.py"
      to: "predictive_poultry_systems.objects.processes.base"
      via: "process model application"
---

<objective>
Implement the behavioral logic for the primary simulation agents (Customers and Staff) using salabim.Component. This bridges the static models from Phase 2 with active simulation processes, enabling the "avian asset" fulfillment cycle.

Purpose: To provide the active decision-making and labor units required for the end-to-end simulation.
Output: Customer and Staff salabim components with defined process logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-cycle-integration/04-RESEARCH.md
@src/predictive_poultry_systems/agents/customers/base.py
@src/predictive_poultry_systems/agents/staff/base.py
@src/predictive_poultry_systems/objects/processes/base.py

<interfaces>
From src/predictive_poultry_systems/agents/customers/base.py:
```python
class BaseCustomer(BaseModel):
    segment: CustomerSegment
    satisfaction: float
    price_sensitivity: float
    loyalty: CustomerLoyalty
```

From src/predictive_poultry_systems/agents/staff/base.py:
```python
class StaffRoles(StrEnum):
    CASHIER = "cashier"
    FRY_COOK = "fry_cook"
    GRILL_MASTER = "grill_master"

class BaseStaff(BaseModel):
    name: str
    role: StaffRoles
    skill_level: float
    fatigue_rate: float
```

From src/predictive_poultry_systems/objects/processes/base.py:
```python
class ThermodynamicProcess(BaseModel):
    duration_mean: float
    target_temp_c: float

class AssemblyProcess(BaseModel):
    duration_mean: float
    fte_requirement: float
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Customer behavior component</name>
  <files>src/predictive_poultry_systems/agents/customers/behavior.py</files>
  <behavior>
    - Customer class must inherit from `sim.Component`.
    - It should accept a `BaseCustomer` object for its parameters.
    - The `process()` method should model:
        1. Arrival (waiting in queue).
        2. Ordering (requesting a kiosk resource).
        3. Waiting for food (waiting for a signal or store availability).
        4. Consumption (hold for a duration).
        5. Exit (leaving the simulation).
  </behavior>
  <action>
    Create `src/predictive_poultry_systems/agents/customers/behavior.py`.
    Implement the `Customer` class as a `salabim.Component`.
    Use the `Relay` pattern (Pattern 1) where the customer drives its own lifecycle.
    Integrate `sim.Exponential` for inter-arrival times based on segment profiles.
  </action>
  <verify>
    <automated>uv run python -c "from predictive_poultry_systems.agents.customers.behavior import Customer; print('Import successful')"</automated>
  </verify>
  <done>Customer component is defined with stochastic arrival and lifecycle logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Staff behavior component</name>
  <files>src/predictive_poultry_systems/agents/staff/behavior.py</files>
  <behavior>
    - Staff class must inherit from `sim.Component`.
    - It should accept a `BaseStaff` object for its parameters.
    - The `process()` method should model:
        1. Idle state (waiting for an order).
        2. Order fulfillment (checking `holding_cabinet` for product).
        3. Production (if product is missing, execute a `ThermodynamicProcess`).
        4. Delivery (placing item in delivery zone or store).
    - It should account for `skill_level` to modify process durations.
  </behavior>
  <action>
    Create `src/predictive_poultry_systems/agents/staff/behavior.py`.
    Implement the `Staff` class as a `salabim.Component`.
    Staff should interact with `sim.Resource` (machines) and `sim.Store` (holding cabinets).
    Use fatigue rate to slowly degrade `skill_level` over time (using `env.now()`).
  </action>
  <verify>
    <automated>uv run python -c "from predictive_poultry_systems.agents.staff.behavior import Staff; print('Import successful')"</automated>
  </verify>
  <done>Staff component is defined with task execution logic tied to efficiency metrics.</done>
</task>

</tasks>

<verification>
### Agent Logic Verification
- [ ] Components inherit from `salabim.Component`.
- [ ] Components can be instantiated with Pydantic models from Phase 2.
- [ ] Basic process logic (yield statements) exists for both Customer and Staff.
</verification>

<success_criteria>
- Customer and Staff behavioral agents are implemented and importable.
- Agent logic is tied to the physical/operational constraints defined in Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cycle-integration/04-01-SUMMARY.md`
</output>
