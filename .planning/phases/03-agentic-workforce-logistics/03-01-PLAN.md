---
phase: 03-agentic-workforce-logistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, src/predictive_poultry_systems/agents/behavior/bt/base.py, src/predictive_poultry_systems/agents/behavior/bt/composites.py, src/predictive_poultry_systems/agents/behavior/bt/leaves.py, src/predictive_poultry_systems/agents/behavior/llm/providers.py, src/predictive_poultry_systems/agents/behavior/llm/agents.py, tests/test_behavior_framework.py]
autonomous: true
requirements: [AGT-CUST-01, AGT-CUST-02, AGT-STAF-01]
user_setup:
  - service: LLM Providers
    why: "Complex decisions require LLM inference"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter Dashboard (optional, for cloud models)"
      - name: OLLAMA_BASE_URL
        source: "Local Ollama instance (optional, for local models)"

must_haves:
  truths:
    - "Behavior Tree nodes can be serialized/deserialized as Pydantic models"
    - "pydantic-ai can call multiple backends via configurable providers"
    - "BT nodes correctly transition between SUCCESS, FAILURE, and RUNNING states"
  artifacts:
    - path: "src/predictive_poultry_systems/agents/behavior/bt/base.py"
      provides: "Core BT Node abstraction"
      contains: ["class Node(BaseModel)", "def tick"]
    - path: "src/predictive_poultry_systems/agents/behavior/llm/providers.py"
      provides: "Provider-agnostic AI configuration"
      contains: ["get_model"]
    - path: "tests/test_behavior_framework.py"
      provides: "Verification of core logic"
  key_links:
    - from: "src/predictive_poultry_systems/agents/behavior/bt/base.py"
      to: "pydantic"
      via: "BaseModel inheritance"
    - from: "src/predictive_poultry_systems/agents/behavior/llm/agents.py"
      to: "pydantic_ai"
      via: "Agent definition"
---

<objective>
Implement a Pydantic-native minimal Behavior Tree (BT) framework and a provider-agnostic pydantic-ai integration layer. This foundation will enable agents to make structured rules-based decisions and hybrid LLM-driven complex decisions.

Purpose: Decouple decision logic from simulation time and provide a standard interface for agent brains.
Output: Core BT node implementations, AI provider configuration, and unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-agentic-workforce-logistics/03-RESEARCH.md
@.planning/phases/03-agentic-workforce-logistics/03-CONTEXT.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic-native Behavior Tree Framework</name>
  <files>pyproject.toml, src/predictive_poultry_systems/agents/behavior/bt/base.py, src/predictive_poultry_systems/agents/behavior/bt/composites.py, src/predictive_poultry_systems/agents/behavior/bt/leaves.py</files>
  <action>
    1. Add `pydantic-ai` and `logfire` (optional but recommended for pydantic-ai) to `pyproject.toml` and run `uv sync`.
    2. Implement core BT structures as Pydantic models:
       - `base.py`: Define `Status` enum (SUCCESS, FAILURE, RUNNING) and `Node` abstract base model with `tick(context)` method.
       - `composites.py`: Implement `Sequence` and `Selector` (Fallback) nodes. Ensure they track child progress to support `RUNNING` state across ticks.
       - `leaves.py`: Implement `ActionNode` and `ConditionNode` base classes.
    3. Ensure all nodes are serializable via Pydantic for future persistence.
  </action>
  <verify>
    <automated>uv run python -c "from predictive_poultry_systems.agents.behavior.bt.base import Node; from predictive_poultry_systems.agents.behavior.bt.composites import Sequence; print('BT models loadable')"</automated>
  </verify>
  <done>Core BT framework is implemented using Pydantic models, supporting sequential and fallback control flow with execution state tracking.</done>
</task>

<task type="auto">
  <name>Task 2: Provider-agnostic pydantic-ai Integration</name>
  <files>src/predictive_poultry_systems/agents/behavior/llm/providers.py, src/predictive_poultry_systems/agents/behavior/llm/agents.py</files>
  <action>
    1. Create `providers.py` to handle model instantiation based on environment configuration (per D-06).
       - Support `ollama`, `openrouter`, and `openai` (as fallback) providers.
       - Implement a `get_model(model_id: str)` helper that returns a `pydantic_ai.models.Model` instance.
    2. Create `agents.py` to define base agentic behaviors:
       - Use `pydantic_ai.Agent` for structured decision tasks.
       - Define a `DecisionAgent` wrapper that handles common prompting and dependency injection patterns.
  </action>
  <verify>
    <automated>ls src/predictive_poultry_systems/agents/behavior/llm/providers.py src/predictive_poultry_systems/agents/behavior/llm/agents.py</automated>
  </verify>
  <done>AI integration layer is ready, allowing agents to use smol models or cloud APIs via a unified pydantic-ai interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Framework Verification</name>
  <files>tests/test_behavior_framework.py</files>
  <behavior>
    - Sequence node returns SUCCESS only if all children succeed.
    - Sequence node returns RUNNING if a child is RUNNING, and resumes from that child on next tick.
    - Selector node returns SUCCESS if any child succeeds.
    - Condition nodes correctly gate execution.
    - AI providers can be mocked using pydantic-ai's TestModel.
  </behavior>
  <action>
    Create `tests/test_behavior_framework.py` and implement comprehensive unit tests for the BT and LLM layers. Use mocks for network calls.
  </action>
  <verify>
    <automated>uv run pytest tests/test_behavior_framework.py</automated>
  </verify>
  <done>The core behavior framework is verified to handle state management and AI integration correctly.</done>
</task>

</tasks>

<verification>
- [ ] `uv run pytest tests/test_behavior_framework.py` passes 100%.
- [ ] BT nodes correctly serialize to JSON.
- [ ] AI providers switch correctly based on environment variables.
</verification>

<success_criteria>
- Custom Pydantic-native BT framework implemented and tested.
- pydantic-ai integrated with provider-agnostic configuration.
- Base architecture for hybrid rules/LLM decision making established.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentic-workforce-logistics/03-01-SUMMARY.md`
</output>
