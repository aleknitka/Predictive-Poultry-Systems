---
phase: 03-agentic-workforce-logistics
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/predictive_poultry_systems/agents/customers/behavior.py, src/predictive_poultry_systems/agents/staff/behavior.py, src/predictive_poultry_systems/agents/customers/base.py, src/predictive_poultry_systems/agents/staff/base.py, tests/test_agent_behavior.py]
autonomous: true
requirements: [AGT-CUST-01, AGT-CUST-02, AGT-STAF-01]

must_haves:
  truths:
    - "Customer agents can choose menu items based on segment/loyalty context using LLM"
    - "Staff agents can prioritize tasks based on role and current facility state"
    - "BT structures are stored within the Pydantic agent models"
  artifacts:
    - path: "src/predictive_poultry_systems/agents/customers/behavior.py"
      provides: "Customer-specific BT nodes"
    - path: "src/predictive_poultry_systems/agents/staff/behavior.py"
      provides: "Staff-specific BT nodes"
    - path: "tests/test_agent_behavior.py"
      provides: "Verification of agent decision logic"
  key_links:
    - from: "src/predictive_poultry_systems/agents/customers/base.py"
      to: "src/predictive_poultry_systems/agents/behavior/bt/base.py"
      via: "inclusion of root_node field"
---

<objective>
Define specific behavior trees and hybrid decision logic for Customer and Staff agents. Update the base Pydantic models to incorporate these behavioral "brains".

Purpose: Enable agents to exhibit complex, context-aware behaviors (segmentation, loyalty, labor efficiency).
Output: Behavioral BT definitions for customers and staff, updated base models, and unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-agentic-workforce-logistics/03-01-PLAN.md
@src/predictive_poultry_systems/agents/customers/base.py
@src/predictive_poultry_systems/agents/staff/base.py
</context>

<interfaces>
From src/predictive_poultry_systems/agents/behavior/bt/base.py:
```python
class Status(str, Enum):
    SUCCESS = "SUCCESS"
    FAILURE = "FAILURE"
    RUNNING = "RUNNING"

class Node(BaseModel):
    name: str
    def tick(self, agent: Any) -> Status: ...
```

From src/predictive_poultry_systems/agents/behavior/llm/agents.py:
```python
class DecisionAgent:
    async def decide(self, task: str, context: dict) -> Any: ...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Customer and Staff Behavior Trees</name>
  <files>src/predictive_poultry_systems/agents/customers/behavior.py, src/predictive_poultry_systems/agents/staff/behavior.py</files>
  <action>
    1. Implement leaf nodes for Customer behavior:
       - `ArriveNode`: Handles facility entry logic.
       - `QueueDecisionNode`: Hybrid node (BT rules for queue length, LLM for "patience vibe").
       - `OrderDecisionNode`: LLM-driven menu selection using `pydantic-ai` (per D-04).
    2. Implement leaf nodes for Staff behavior:
       - `TaskPriorityNode`: Determines next task based on role and facility needs.
       - `FulfillmentNode`: Handles thermodynamic/assembly process steps.
       - `MoraleNode`: LLM-driven morale update based on fatigue and workload.
    3. Define the default tree structures (e.g., `get_default_customer_tree()`) using Sequence and Selector composites.
  </action>
  <verify>
    <automated>uv run python -c "from predictive_poultry_systems.agents.customers.behavior import ArriveNode; print('Customer nodes loadable')"</automated>
  </verify>
  <done>Specific behavior trees for both agent types are defined, incorporating hybrid rule/LLM nodes.</done>
</task>

<task type="auto">
  <name>Task 2: Update Agent Base Models</name>
  <files>src/predictive_poultry_systems/agents/customers/base.py, src/predictive_poultry_systems/agents/staff/base.py</files>
  <action>
    1. Update `BaseCustomer` (in `customers/base.py`):
       - Add `root_node: Node` field (the BT).
       - Add `brain: DecisionAgent` field (for AI decisions).
       - Add `memory: dict` for persisting short-term behavioral state.
    2. Update `BaseStaff` (in `staff/base.py`):
       - Add `root_node: Node` field.
       - Add `brain: DecisionAgent` field.
       - Add `efficiency_modifier: float` (derived from skill and fatigue).
    3. Ensure both models use `ConfigDict(arbitrary_types_allowed=True)` if needed for BT nodes.
  </action>
  <verify>
    <automated>uv run python -c "from predictive_poultry_systems.agents.customers.base import BaseCustomer; print('BaseCustomer updated')"</automated>
  </verify>
  <done>Base agent models are now "behavioral", containing the necessary structures to execute their BTs and AI agents.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Behavioral Logic Verification</name>
  <files>tests/test_agent_behavior.py</files>
  <behavior>
    - Customer chooses "Champion" menu items when loyalty is high.
    - Customer leaves queue if length exceeds patience (BT rule).
    - Staff switches tasks when a higher priority one appears (e.g., grill needs tending).
    - LLM decisions correctly update agent state (satisfaction/morale).
  </behavior>
  <action>
    Create `tests/test_agent_behavior.py` to verify that agents correctly execute their trees and handle hybrid decisions.
  </action>
  <verify>
    <automated>uv run pytest tests/test_agent_behavior.py</automated>
  </verify>
  <done>Agent behavioral logic is verified through unit and mock-AI tests.</done>
</task>

</tasks>

<verification>
- [ ] `uv run pytest tests/test_agent_behavior.py` passes 100%.
- [ ] Customer BT correctly handles segment-based decision logic.
- [ ] Staff BT correctly handles role-based task switching.
</verification>

<success_criteria>
- Specific behavior trees implemented for customers and staff.
- Pydantic models (BaseCustomer, BaseStaff) updated to include behavioral structures.
- Complex decisions (menu, satisfaction, morale) successfully handled via hybrid BT/LLM logic.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agentic-workforce-logistics/03-02-SUMMARY.md`
</output>
