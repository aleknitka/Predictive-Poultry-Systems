---
phase: 05-throughput-optimization
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified: [src/predictive_poultry_systems/agents/behavior/metrics.py, tests/test_behavior_metrics.py]
autonomous: true
requirements: [MET-02, MET-03]
must_haves:
  truths:
    - "Satisfaction decay follows exponential decay based on wait time."
    - "Crisp-state follows a Gaussian distribution centered at 240s."
    - "Morale calculation accounts for fatigue and skill level."
  artifacts:
    - path: "src/predictive_poultry_systems/agents/behavior/metrics.py"
      provides: "Core fulfillment metrics formulas"
    - path: "tests/test_behavior_metrics.py"
      provides: "Unit tests for fulfillment metrics"
  key_links:
    - from: "tests/test_behavior_metrics.py"
      to: "src/predictive_poultry_systems/agents/behavior/metrics.py"
      via: "import"
---

<objective>
Implement the mathematical foundations for performance optimization, including customer satisfaction (CSI), staff morale (SMI), and thermodynamic "Crisp-state" compliance.

Purpose: Provide objective functions for fulfillment optimization.
Output: Validated formula module for behavioral and physical performance.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-throughput-optimization/05-RESEARCH.md
@src/predictive_poultry_systems/agents/customers/base.py
@src/predictive_poultry_systems/agents/staff/base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Thermodynamic Crisp-state Formula</name>
  <files>src/predictive_poultry_systems/agents/behavior/metrics.py, tests/test_behavior_metrics.py</files>
  <behavior>
    - calculate_crisp_state(cook_time=240) -> ~1.0
    - calculate_crisp_state(cook_time=180) -> < 0.5
    - calculate_crisp_state(cook_time=300) -> < 0.5
  </behavior>
  <action>
    Create a Gaussian-based scoring function (mu=240, sigma=20) to evaluate how close a cooked asset is to its thermodynamic optimum.
    Formula: score = exp(-((cook_time - mu)**2) / (2 * sigma**2)).
    Implementation in `src/predictive_poultry_systems/agents/behavior/metrics.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_behavior_metrics.py::test_calculate_crisp_state</automated>
  </verify>
  <done>Formula returns 1.0 at 240s and decays appropriately on both sides.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Customer Satisfaction (CSI) Decay Formula</name>
  <files>src/predictive_poultry_systems/agents/behavior/metrics.py, tests/test_behavior_metrics.py</files>
  <behavior>
    - update_satisfaction(wait_time=180, base=10.0) -> 10.0 (baseline wait)
    - update_satisfaction(wait_time=300, base=10.0) -> ~5.4 (k=0.005)
  </behavior>
  <action>
    Implement exponential satisfaction decay based on wait time (SoS).
    Formula: base_satisfaction * exp(-k * max(0, wait_time - expected_sos)).
    Use k=0.005 and expected_sos=180.
    Implementation in `src/predictive_poultry_systems/agents/behavior/metrics.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_behavior_metrics.py::test_update_satisfaction</automated>
  </verify>
  <done>Satisfaction decays exponentially after the 180s threshold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Staff Morale (SMI) Formula</name>
  <files>src/predictive_poultry_systems/agents/behavior/metrics.py, tests/test_behavior_metrics.py</files>
  <behavior>
    - calculate_morale(fatigue=0.0, skill=1.0) -> 10.0
    - calculate_morale(fatigue=0.5, skill=0.5) -> < 5.0
  </behavior>
  <action>
    Implement a formula for staff morale that balances fatigue and skill level.
    Morale = 10.0 * (1.0 - fatigue) * skill. (Simple linear model for now).
    Implementation in `src/predictive_poultry_systems/agents/behavior/metrics.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_behavior_metrics.py::test_calculate_morale</automated>
  </verify>
  <done>Morale correctly reflects the impact of fatigue and skill.</done>
</task>

</tasks>

<verification>
Run unit tests for all metrics:
`uv run pytest tests/test_behavior_metrics.py`
</verification>

<success_criteria>
- Core metric formulas are implemented in `metrics.py`.
- Unit tests pass for all three core metrics (CSI, SMI, Crispness).
- Mathematical behavior matches research definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-throughput-optimization/05-01-SUMMARY.md`
</output>
