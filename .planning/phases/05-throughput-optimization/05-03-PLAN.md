---
phase: 05-throughput-optimization
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [src/predictive_poultry_systems/analytics/reporter.py, main.py, tests/test_fulfillment_audit.py]
autonomous: false
requirements: [MET-01, MET-03]
must_haves:
  truths:
    - "A readable 'Fulfillment Audit' is printed at the end of the simulation."
    - "The audit includes throughput, fiscal yield, and crispness compliance."
    - "Wait-time histograms are generated automatically."
  artifacts:
    - path: "src/predictive_poultry_systems/analytics/reporter.py"
      provides: "Aggregation and reporting logic for simulation monitors"
    - path: "tests/test_fulfillment_audit.py"
      provides: "Integration test for end-of-sim reporting"
  key_links:
    - from: "main.py"
      to: "src/predictive_poultry_systems/analytics/reporter.py"
      via: "import and execution"
---

<objective>
Implement the "Fulfillment Audit" reporter to aggregate statistics from salabim monitors and provide human-readable performance insights, including fiscal yield and crisp-state compliance.

Purpose: Generate actionable insights from simulation runs.
Output: Integrated reporting tool and validated audit output.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-throughput-optimization/05-RESEARCH.md
@src/predictive_poultry_systems/agents/simulation.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Fulfillment Reporter</name>
  <files>src/predictive_poultry_systems/analytics/reporter.py</files>
  <action>
    - Create `src/predictive_poultry_systems/analytics/reporter.py`.
    - Implement a `generate_fulfillment_audit(env)` function.
    - Function should:
      - Extract and print mean, std dev, and histograms for `Revenue`, `SoS`, `Satisfaction`, and `Morale`.
      - Calculate Throughput (orders per simulated hour).
      - Print a "Thermodynamic Summary" (average crisp-state score).
      - Use `Monitor.print_histogram()` for built-in salabim formatting.
  </action>
  <verify>
    <automated>ls src/predictive_poultry_systems/analytics/reporter.py</automated>
  </verify>
  <done>Reporter module created with audit generation capabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Reporter into Main Loop</name>
  <files>main.py</files>
  <action>
    - Update `run_simulation()` in `main.py` to call `generate_fulfillment_audit(env)` after `env.run(till=till)`.
    - Ensure `till` is long enough to produce meaningful statistics (e.g., 200).
    - Ensure all cooking events tally crisp-state compliance into a monitor held by `env.fulfillment_manager`.
  </action>
  <verify>
    <automated>grep -q "generate_fulfillment_audit" main.py</automated>
  </verify>
  <done>Main simulation entry point triggers the performance audit upon completion.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify Fulfillment Audit Output</name>
  <what-built>End-to-end fulfillment audit generation.</what-built>
  <how-to-verify>
    Run: `uv run main.py --till 200`
    Expected behavior:
    - Simulation runs with trace output.
    - At the end, a "Fulfillment Audit" section appears in the console.
    - It contains statistical breakdowns for Revenue, SoS, Satisfaction, and Morale.
    - Histograms are printed for SoS.
  </how-to-verify>
  <resume-signal>Type "approved" if the audit output is comprehensive and readable.</resume-signal>
</task>

</tasks>

<verification>
Run the integration test:
`uv run pytest tests/test_fulfillment_audit.py`
</verification>

<success_criteria>
- Fulfillment audit prints automatically after simulation completion.
- Reports mean/std for 4+ KPIs.
- Throughput is calculated in "units per hour."
- Salabim histograms are correctly formatted in console output.
</success_criteria>

<output>
After completion, create `.planning/phases/05-throughput-optimization/05-03-SUMMARY.md`
</output>
