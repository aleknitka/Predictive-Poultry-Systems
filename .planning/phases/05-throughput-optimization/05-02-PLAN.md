---
phase: 05-throughput-optimization
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/predictive_poultry_systems/agents/simulation.py, main.py]
autonomous: true
requirements: [MET-01, MET-02]
must_haves:
  truths:
    - "FulfillmentManager exists in simulation.py and is used by the environment."
    - "Sim monitors are tallying revenue, wait time, and satisfaction."
    - "Revenue increments on order placement/fulfillment."
  artifacts:
    - path: "src/predictive_poultry_systems/agents/simulation.py"
      provides: "Centralized FulfillmentManager with salabim Monitors"
  key_links:
    - from: "main.py"
      to: "src/predictive_poultry_systems/agents/simulation.py"
      via: "import and instantiation"
---

<objective>
Promote the FulfillmentManager to a core orchestrator in simulation.py, implement salabim monitors for real-time performance tracking, and integrate metrics tallying into agent components.

Purpose: Automate data collection for throughput and stakeholder friction.
Output: Integrated monitoring framework within the simulation environment.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-throughput-optimization/05-RESEARCH.md
@src/predictive_poultry_systems/agents/simulation.py
@main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote and Enhance FulfillmentManager</name>
  <files>src/predictive_poultry_systems/agents/simulation.py, main.py</files>
  <action>
    - Relocate `FulfillmentManager` class from `main.py` to `src/predictive_poultry_systems/agents/simulation.py`.
    - Update `FulfillmentManager.setup()` to initialize `salabim` Monitors:
      - `self.revenue_monitor = sim.Monitor("Revenue", level=True)`
      - `self.sos_monitor = sim.Monitor("Speed of Service")`
      - `self.satisfaction_monitor = sim.Monitor("Customer Satisfaction", level=True)`
      - `self.morale_monitor = sim.Monitor("Staff Morale", level=True)`
    - Add a `tally_revenue(amount)` and `tally_satisfaction(value)` methods to `FulfillmentManager`.
    - Update `main.py` to import `FulfillmentManager` from `simulation.py`.
  </action>
  <verify>
    <automated>grep -q "revenue_monitor" src/predictive_poultry_systems/agents/simulation.py && grep -q "from .simulation import FulfillmentManager" main.py</automated>
  </verify>
  <done>FulfillmentManager is relocated and enhanced with monitors.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Metrics Tallying into Components</name>
  <files>src/predictive_poultry_systems/agents/simulation.py</files>
  <action>
    - Update `BehavioralComponent.process()` to interact with the environment's `fulfillment_manager`.
    - When a `CustomerComponent` finishes its order process:
      - Tally revenue in `fulfillment_manager`.
      - Record speed of service (SoS) using `self.env.now() - arrival_time`.
      - Calculate and tally satisfaction using `update_satisfaction` from `metrics.py`.
    - Periodically (or on task completion) tally staff morale for `StaffComponent` using `calculate_morale`.
  </action>
  <verify>
    <automated>grep -q "tally_revenue" src/predictive_poultry_systems/agents/simulation.py</automated>
  </verify>
  <done>Simulation components actively tally metrics into the monitors during execution.</done>
</task>

</tasks>

<verification>
Run the simulation and check if monitors are populated:
`uv run main.py --till 50`
(Should run without error and the trace should show monitor updates if enabled).
</verification>

<success_criteria>
- FulfillmentManager correctly initializes 4 salabim monitors.
- Customer components record SoS and satisfaction upon "fulfillment."
- Staff components record morale periodically.
- Revenue is tracked as a level monitor.
</success_criteria>

<output>
After completion, create `.planning/phases/05-throughput-optimization/05-02-SUMMARY.md`
</output>
